[package]
name = "generate-catalog"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
shlex = "1.3.0"
tl = "0.7.8"
ureq = { version = "3.3.0", default-features = false, features = ["native-tls"] }
